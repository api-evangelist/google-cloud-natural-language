naftiko: 1.0.0-alpha2
info:
  label: Google Cloud Natural Language API — documents:analyzeEntities
  description: 'Google Cloud Natural Language API — documents:analyzeEntities. 1 operations. Lead operation: Google Cloud
    Natural Language Analyze entities. Self-contained Naftiko capability covering one Google Cloud Natural Language business
    surface.'
  tags:
  - Google Cloud Natural Language
  - documents:analyzeEntities
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GOOGLE_CLOUD_NATURAL_LANGUAGE_API_KEY: GOOGLE_CLOUD_NATURAL_LANGUAGE_API_KEY
capability:
  consumes:
  - type: http
    namespace: openapi-documents-analyzeentities
    baseUri: https://language.googleapis.com/v1
    description: Google Cloud Natural Language API — documents:analyzeEntities business capability. Self-contained, no shared
      references.
    resources:
    - name: documents:analyzeEntities
      path: /documents:analyzeEntities
      operations:
      - name: analyzeentities
        method: POST
        description: Google Cloud Natural Language Analyze entities
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: openapi-documents-analyzeentities-rest
    port: 8080
    description: REST adapter for Google Cloud Natural Language API — documents:analyzeEntities. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/documents-analyzeentities
      name: documents-analyzeentities
      description: REST surface for documents:analyzeEntities.
      operations:
      - method: POST
        name: analyzeentities
        description: Google Cloud Natural Language Analyze entities
        call: openapi-documents-analyzeentities.analyzeentities
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: openapi-documents-analyzeentities-mcp
    port: 9090
    transport: http
    description: MCP adapter for Google Cloud Natural Language API — documents:analyzeEntities. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: google-cloud-natural-language-analyze
      description: Google Cloud Natural Language Analyze entities
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: openapi-documents-analyzeentities.analyzeentities
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
